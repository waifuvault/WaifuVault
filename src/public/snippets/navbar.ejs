<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Waifu Vault</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="nav nav-pills">
                <li class="nav-item">
                    <a class="nav-link" id="filesNav" href="<% if(loginType==="user") { %>/admin/files<% } else { %>/admin/bucket<% } %>">File Management</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="statsNav" href="<% if(loginType==="user") { %>/admin/stats<% } else { %>/admin/bucket/stats<% } %>">Statistics</a>
                </li>
                <% if(loginType==="user") { %>
                    <li class="nav-item">
                        <a class="nav-link" id="userNav" href="/admin/user">User</a>
                    </li>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link" href="<% if(loginType==="user") { %>/rest/auth/logout<% } else { %>/rest/auth/close_bucket<% } %>">
                        <% if(typeof bucket === "undefined"){ %>Logout
                        <% }else{ %>Close bucket
                        <% } %>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
