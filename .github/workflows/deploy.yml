name: Deploy

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  fetch-frontend-env:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    environment: production
    outputs:
      frontend-env: ${{ steps.fetch.outputs.env }}
    steps:
      - name: Fetch frontend .env from server
        id: fetch
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          port: 1222
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: cat ${{ secrets.DEPLOY_PATH }}/frontend/waifu-vault/.env

  build-images:
    needs: fetch-frontend-env
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - service: waifuvault
            dockerfile: Dockerfile
            target: production
          - service: frontend
            dockerfile: frontend/waifu-vault/Dockerfile
            target: ""
          - service: thumbnails
            dockerfile: go/thumbnails/Dockerfile
            target: ""
          - service: zipfiles
            dockerfile: go/zipFiles/Dockerfile
            target: ""
    env:
      IMAGE: ghcr.io/${{ github.repository }}/${{ matrix.service }}
    steps:
      - uses: actions/checkout@v6

      - name: Write frontend .env
        if: matrix.service == 'frontend'
        run: echo "${{ needs.fetch-frontend-env.outputs.frontend-env }}" > frontend/waifu-vault/.env

      - name: Lowercase image name
        run: echo "IMAGE=${IMAGE,,}" >> "$GITHUB_ENV"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          target: ${{ matrix.target || '' }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

  deploy:
    needs: build-images
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          port: 1222
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}
            git pull
            docker compose pull
            docker compose up -d --remove-orphans
